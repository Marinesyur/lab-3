#include <iostream>
using namespace std; 
int main() {
    int v;
    cin >> v;
    int a = (v / 1000);
    int b = (v / 100 % 10);
    int c = (v / 10 % 10);
    int d = (v % 10);
    int N = (a - d) * (a - d);
    int M = (b - c) * (b - c);
    cout << (N + M) + 1;
    return 0;
}
